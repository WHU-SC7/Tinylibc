# tcc — 完整 C 编译器（独立运行时，不依赖 tlibc.a）
tcc: tcc_rt tcc_rt_start lex parse preproc cgen cgen_expr cgen_asm elf_write

# tpp — 独立预处理器（独立运行时，不依赖 tlibc.a）
tpp: tcc_rt tcc_rt_start preproc

# tas — x86_64 汇编器（独立运行时，不依赖 tlibc.a）
tas: tcc_rt tcc_rt_start elf_write
